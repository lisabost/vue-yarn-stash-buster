function Yarn(name, length, color, weight, fiber) {
    this.name = name || 'Yarn Name';
    this.length = length || 0;
    this.color = color || '';
    this.weight = weight || '';
    this.fiber = fiber || '';
}

export default Yarn;